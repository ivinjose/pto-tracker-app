import { View } from 'react-native';

/**
 * Placeholder screen for the Logout tab.
 * Actual logout is triggered by the custom tabBarButton in _layout.tsx.
 */
export default function LogoutScreen() {
	return <View />;
}
