/** Train booking types (e.g. general, tatkal). */
export const TRAIN_TYPES = ['general', 'tatkal', 'premium_tatkal'];

/** Days before travel date when general train booking opens (e.g. 60 days for IRCTC). */
export const TRAIN_BOOKING_BUFFER = 60;

/** Time of day when general train booking opens (e.g. "08:00" AM). */
export const TRAIN_NORMAL_BOOKING_OPENING_TIME = "08:00";

/** Time of day when tatkal booking opens (e.g. "10:00" AM). */
export const TRAIN_TATKAL_BOOKING_OPENING_TIME = "10:00";
